{
  "name": "ls",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test-new": "playwright test --browser=chromium --headed --reporter=html --screenshot=only-on-failure --video=retain-on-failure",
    "test": "playwright test --browser=chromium --headed --reporter=html"
  },
  "dependencies": {
    "@playwright/test": "^1.51.1"
  }
}
